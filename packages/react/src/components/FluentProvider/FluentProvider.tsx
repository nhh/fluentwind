import { FluentWindProvider } from '../../provider';
import type { FluentWindProviderProps } from '../../provider';

/**
 * FluentProvider is an alias of FluentWindProvider for Fluent UI API compatibility.
 */
export const FluentProvider = FluentWindProvider;

export type FluentProviderProps = FluentWindProviderProps;
